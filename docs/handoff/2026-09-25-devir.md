# Devir notu — 25 Eylül 2026

Üç ayrı okuyucuya üç bölüm. **§ A** sonraki terminal Claude oturumuna,
**§ B** Hakan'a, **§ C** Cowork Claude'a.

---

# § A — SONRAKİ TERMİNAL CLAUDE OTURUMUNA

## A.1 Durum — ÖLÇÜLDÜ

### Depo

```
HEAD          : 674e3e3
origin/main   : 95f93e1
git status -sb: ## main...origin/main [ahead 3]
çalışma ağacı : temiz (0 değişiklik)
```

> ### ⚠ ÜÇ COMMIT PUSH EDİLMEDİ
> `e200900` · `8d81f12` · `674e3e3` yerelde duruyor, GitHub'da yok.
> GitHub `pushed_at` = `2026-09-24T21:49:10Z`, yani `95f93e1`'in push'u.
>
> **Bu devir notunun commit'iyle birlikte push edilecek.** Oturum açan
> ajanın ilk işi durumu yeniden ölçmek olsun — arada push edilmiş olabilir:
> ```bash
> git status -sb | head -1
> ```

### Zincir — blok 11775088 · 2026-09-24 22:41:13 UTC

| | |
|---|---|
| PQWallet `nonce()` | **7** |
| PQWallet bakiyesi | `50400000000000000` wei · 0,0504 ETH |
| ödeyen EOA bakiyesi | 0,046285709934898207 ETH |
| uç | `ethereum-sepolia-rpc.publicnode.com` |

Nonce 7 **ayrılmamıştır** — bir sonraki iş için serbest.

## A.2 Bu oturumda kapananlar

Sayılar burada tekrarlanmaz; her satırın tek kaynağı sağdaki dosyadır.

| iş | kanıt |
|---|---|
| Video öncesi UI geçişi (sayı biçimi, DURUM etiketleri, receipt etiketi) | `crypto-tests/sprint4-number-format-and-status-labels.md` |
| Kayıtlı demo koşusu — nonce 5, Task 7'nin **B** satırı | `crypto-tests/sprint4-recorded-demo-run.md` |
| **C** satırının ölçümü — nonce 6 | `crypto-tests/sprint4-c-row-measurement.md` |
| Task 7 dört satırlık tablo (plan Adım 3) + eski `:290-300` sapmasının kaynağı | `crypto-tests/sprint4-gas-table-and-second-tx.md`, 25 Eylül tarihli ekleri |

Ön kayıtlar: `evidence/demo-nonce5-prerecord.md`, `evidence/c-nonce6-prerecord.md`.
Akış kâğıtları: `evidence/demo-run-sheet.md`, `evidence/c-run-sheet.md`.

**Sprint 4'ün K2 kalemi kapandı.** Üç gas satırı da artık zincirde ölçülmüş.

## A.3 Açık kalemler — öncelik sırasıyla

| # | kalem | kimin işi | rapor için engel mi |
|---|---|---|---|
| 1 | **EIP referans teyidi** — 2.500/25.000/12 gas hangi kaynağa bağlandı; bu oturumda spec metnine karşı **doğrulanmadı**, hafızadan yazıldı | **Hakan** (§ B.2) | **EVET** — rapora kaynaksız sayı giremez |
| 2 | **README gas tablosu** eski (`233.429` + tahmini `258.429`) | **Hakan** (dosyası) | **EVET** — jüri README'ye bakar |
| 3 | **Rapor yazılmadı** (A.4) | Akif + ajan | **EVET** — işin kendisi |
| 4 | **−1 gas**, ham tahmin yolunda: B −1, C 0. Sebep ölçülmedi | Akif | hayır — açık kalem olarak yazılır |
| 5 | **WASM çapraz-makine determinizmi ölçülmedi.** Hakan hash'ledi ama **derlemedi**; ölçülen şey git'in aktarım sadakatiydi | **Hakan** (§ B.3) | hayır — "ölçülmedi" diye yazılabilir |
| 6 | **`DURUM: ZİNCİRDE REVERT` ve `SONUÇ ALINAMADI` hiç gözlenmedi** — yalnız kodda | Akif | hayır — "sınanmamış yollar"a girer |
| 7 | **Kanarya betikli değil**, görsel kontrol | Akif | hayır |
| 8 | **`.gitattributes` YOK** (ÖLÇÜLDÜ). `*.wasm binary` önerisi; bozulmama şu an git'in kendi sezimiyle, yazılı kuralla değil | Akif | hayır |
| 9 | **"22" belirsizliği** — Hakan *"22'ye kadar PQWallet'a dokunmuyorum"* dedi, tarih mi task no mu belirsiz | Hakan (§ B'de sorulmadı) | hayır |
| 10 | **"Added protection" ile Smart account şalteri ayrılmadı** — nonce 4'te tek değişkenli deney yapılmadı | Akif | hayır |

## A.4 SIRADAKİ İŞ: rapor

**Teslim 30 Eylül 2026** (`plans/2026-09-14-sprint4-demo-measurement-report.md:52`,
17 Eylül'de teyit edildi). 30 Eylül öncesi ayrı bir rapor teslimi **yok**
(`:57`); tarih finalin kendisi.

### Ne kadarı yazıldı — ÖLÇÜM, tahmin değil

```
$ ls -la docs/RAPOR.md
ls: docs/RAPOR.md: No such file or directory

$ git log --oneline --all -- docs/RAPOR.md
(çıktı yok)
```

**Rapor dosyası hiç oluşturulmadı.** Git geçmişinde de yok. Yazılan **0 satır**.

### Var olan tek hazırlık

`docs/RAPOR_HAM_ICERIK.md` — **166 satır**, yedi bölüm. Ama kendi başlığı
şöyle diyor (`:1`, `:3-7`):

> *"Rapor için ham teknik içerik (**Hakan'ın tarafı**) … Bu belge bir rapor
> bölümü DEĞİL — Akif'in rapor yazarken kullanacağı ham malzeme."*

Yani bu iskelet değil, **Hakan'ın tarafının hammaddesi**.

### İskelet nerede tarif edilmiş

| | dosya:satır |
|---|---|
| **Task 11** — Rapor iskeleti + bölüm→kanıt haritası; `docs/RAPOR.md` **oluşturulacak** | `plans/2026-09-14-sprint4-demo-measurement-report.md:1469-1472` |
| **Task 12** — Raporun **Akif bölümleri**; `docs/RAPOR.md` değiştirilecek | aynı dosya `:1517-1520` |
| Akif bölümlerinin listesi | `:1524-1525` |

### Akif'in bölümleri (plan `:1524-1525`)

C13 ve neden seçildiği · verifier ve *"asla revert etmez"* sözleşmesi ·
imza yolu (BIP-39/44 → WASM → calldata) · digest formatı ve donmuşluğu ·
üç kalkan · gas defteri · sınanmamış yollar.

### Raporda BLOKE bir kalem var

Plan `:1495-1505`: iki gas tablosu çelişiyor — `RAPOR_HAM_ICERIK.md` Böl. 5'in
Foundry sayıları (`88.247`) ile canlı Sepolia (`216.221`) arasında **2,5 kat**
fark. Plana göre rapora `> ⚠️ UZLAŞTIRMA NOTU BEKLİYOR — Hakan'ın cevabı`
bloğu konur ve **sebep iddia edilmez**. Aday açıklamalar (Foundry'nin
intrinsic'i saymaması, `MockVerifier`) Hakan'ın cevabı gelmeden **yazılmaz**.

> Bu oturumda ölçülen `z` ve intrinsic ayrıştırması bu uzlaştırmaya malzeme
> olabilir (intrinsic ≈ 81.000, yürütme ≈ 137.713) — ama **köprüyü kurmak
> hâlâ Hakan'ın cevabına bağlı.** Tahminle kapatma.

## A.5 ÇALIŞMA KURALLARI — bu oturumda oturanlar

1. **Akif commit ve push atar. Claude git'e yazmaz.** Değişiklik hazır
   olunca `git status` + `git diff --stat` göster, commit komutunu ver, DUR.
2. **`--amend` ÖNERME.** Bir kez önerildi, Akif örnek metni birebir koşup
   gerçek bir commit mesajını ezdi. Her zaman **yeni commit**, **tek
   satırlık `-m`**, **en fazla 3 dosya**.
3. **Komutları kopyala-yapıştır ver, numaralı adımlarla.** Kabuk değişkeni
   kullanma (`D=/path` satırı ayrı çalıştırılınca boşa düşüyor, bir kez
   patladı). Mesaj dosyası gerekiyorsa **tam yol** yaz.
4. **Her iddia ÖLÇÜM / ÇIKARIM etiketli.** Görmediğini "çalışıyor" diye
   yazma. Ekran görüntüsüyle belgelenen ile operatör raporu **ayrı** tutulur.
5. **Tarihli ek; yukarısı silinmez.** Yanlış çıkan bir hüküm bile düzeltilmez,
   altına tarihli ek yazılır.
6. **Zincire tx öncesi: ön kayıt + PUSH.** Sıra kanıtı `pushed_at` ile
   alınır, commit tarihiyle değil (commit tarihi istemci üretimi).
   `pushed_at` **son** push'u gösterir — ölçümü **tx'ten önce** oku ve ham
   çıktıyı kanıta yaz, sonraki push'ta kaybolur.
7. **Test eklerken kırmızı/yeşil (mutasyon) kontrolü.** Kodu geçici boz,
   testin kırmızı yandığını göster, geri al, hash'le doğrula. Bu oturumda
   bir assertion'ın iddia ettiğinden azını kanıtladığı böyle yakalandı.
8. **`.env.pqwallet-owner-key` asla okunmaz**, cat edilmez, hiçbir komuta
   verilmez. Mnemonic tarayıcıya **Akif tarafından elle** girilir.
9. **Dokümantasyon commit'lerinde gizli tarama**
   (`node docs/tools/scan-secrets.mjs <dosya>`), **desen desen sayımla**.
   "Temiz" denmez — betiğin kendi uyarısı gereği çıktı bir filtredir;
   her kalem tek tek incelenir.
10. **Klasör sahipliği (CLAUDE.md).** `README.md`, `PQWallet.sol`,
    `Migration.sol`, `contracts/script/` Hakan'ın. Dokunma, ondan iste.

---

# § B — HAKAN'A

Selam Hakan. Sprint 4'ün ölçüm ve demo kısmı bitti. Aşağıda ne yaptığımız ve
senden ne istediğimiz var. Uzun okumak istemezsen **B.2'deki numaralı liste**
yeterli.

## B.1 Ne ölçüldü

Cüzdanın gerçek Sepolia'da üç kez çalıştırıldı, her seferinde **farklı bir
alıcı tipiyle**. Amaç: "bu cüzdan bir işlemde ne kadar yakıt harcıyor"
sorusuna tek bir sayı değil, **koşullu bir tablo** verebilmek.

| alıcı | harcanan (gasUsed) |
|---|---|
| Zaten sıcak olan adres (kendine iade, test düzeneği) | **216.269** |
| Soğuk ama var olan adres — **tipik kullanıcı işlemi** | **218.721** |
| Hiç kullanılmamış boş adres | **243.817** |

Üç satırın arasındaki farklar, calldata maliyeti arındırıldıktan sonra
**EVM'nin kesin sayılarına birebir oturdu**: sırasıyla 2.500 ve 25.000.
Yani modelimiz tuttu, sapma yok.

Ayrıntı: `docs/evidence/crypto-tests/sprint4-c-row-measurement.md` ve
`docs/evidence/crypto-tests/sprint4-gas-table-and-second-tx.md`'nin
25 Eylül tarihli eki.

Ayrıca kayıtlı demo videosu çekildi (kesintisiz tek çekim).

## B.2 Senden istenenler

### 1. README'deki gas tablosunu güncelle

`README.md:133-134` hâlâ eski sayıları taşıyor:
- `233.429` — bu senin 7 Eylül'deki **ilk** işlemindi, tek seferlik bir
  ek maliyet taşıyor, artık tipik değil
- `258.429` — bu bir **tahmindi** ("henüz zincirde ölçülmedi" diye yazılı),
  şimdi gerçeği ölçüldü: **243.817**

**Senin dosyan, ben dokunmadım.** Yeni tabloyu yukarıdaki üç satırdan
kurabilirsin; kaynak olarak kanıt dosyasını göster.

### 2. EIP referanslarını teyit et

Ölçtüğümüz farkları kaynaklara bağladım **ama spec metnine bakmadan,
hafızadan**. Sayılar zincirde ölçüldü, **eşleştirmeler teyide muhtaç**.
Kontrol eder misin:

| sayı | bağladığım kaynak | doğru mu |
|---|---|---|
| **2.500** (soğuk − sıcak hesap erişimi) | **EIP-2929**: `COLD_ACCOUNT_ACCESS_COST` 2600, `WARM_STORAGE_READ_COST` 100 | ☐ |
| **25.000** (boş hesaba para gönderme) | **Yellow Paper** Appendix G, `G_newaccount`; "boş" tanımı **EIP-161** | ☐ |
| **12 gas/bayt** (sıfır olmayan − sıfır bayt) | sıfır-dışı 16 → **EIP-2028**; sıfır 4 → Yellow Paper `G_txdatazero` | ☐ |
| **EIP-7623 taban formülü** `21000 + 10·(z + 4·nz)` | EIP-7623, `TOTAL_COST_FLOOR_PER_TOKEN` = 10 | ☐ |

Son satır şunun için önemli: o taban bizim işlemlerimizde **devreye
girmiyor** (hesabı kanıt dosyasında), ama formülü yanlış yazdıysam sonucu
da yanlış yazmışım demektir.

### 3. WASM derlemesini kendi makinende yapabilir misin? (~15 dk)

22 Eylül'de dosyaların sha256'sını aldın ve bizimkiyle aynı çıktı — ama
**derlemedin**, depodaki hazır dosyayı hash'ledin. Yani ölçülen şey git'in
dosyayı bozmadan taşıdığıydı; **iki makinede aynı kaynaktan aynı çıktı
üretilip üretilmediği hâlâ ölçülmedi.**

Rust ve wasm-pack kurabilirsen:

```
1) Kurulum (bir kereye mahsus)
   https://rustup.rs adresinden rustup kur
   cargo install wasm-pack

2) Derle
   cd pq-safe/frontend
   bash scripts/build-wasm.sh

3) Kontrol et
   bash scripts/verify-wasm.sh
```

3. adım **OK** derse iki makine aynı çıktıyı üretiyor demektir.
**"toolchain farklı, karşılaştırma atlandı"** derse sürümler tutmamıştır —
o çıktıyı bana yolla, hangi sürümü kurman gerektiğini söylerim.

Kuramıyorsan sorun değil, "ölçülmedi" diye rapora yazarız.

### 4. Raporda senin bölümlerin hangileri, ne durumda?

Rapor dosyası (`docs/RAPOR.md`) **henüz açılmadı** — ölçtüm, git geçmişinde
bile yok. Senin tarafının ham malzemesi `docs/RAPOR_HAM_ICERIK.md`'de duruyor
(166 satır, yedi bölüm).

Bana şunu söyle: **hangi bölümleri sen yazacaksın, ne kadarı hazır?**
Teslim 30 Eylül.

> **Bir de bloke bir kalem var:** `RAPOR_HAM_ICERIK.md` Bölüm 5'teki Foundry
> gas sayıları (`88.247`) ile canlı Sepolia ölçümü (`216.221`) arasında
> **2,5 kat** fark var. Sebebini **tahmin etmeyeceğiz**; senin cevabın
> gelmeden rapora köprü yazılmıyor. Foundry'nin intrinsic maliyeti saymaması
> ya da `MockVerifier` kullanılması akla geliyor ama **ölçmedik**. Bakabilir
> misin?

## B.3 Bilgi

Demo işleminde cüzdandan **senin hesabına** (`0x7268a7c3…d075b6`)
**0,0001 ETH** gitti. Test parası, geri istemiyoruz — ama bilmen iyi olur.

Tx: `0x6b8bbecd0bc7fefc36ed5120d09410af7aff970950599652510828c28cd312ff`

---

# § C — COWORK CLAUDE'A (inceleme tarafı)

## C.1 Bu oturumda gelen düzeltmeler ve hangileri yanlış çıktı

Cowork'ten gelen görev kâğıtları birkaç yerde depoda karşılığı olmayan
varsayımlar taşıyordu. Üçü ölçümle düzeltildi:

| varsayım | gerçek |
|---|---|
| *"`render.js` refactor YOK"* — yasak maddesi olarak yazılmıştı | **`render.js` bu depoda hiç yok.** `find . -name "render*"` → çıktı yok. Planlanmış bir dosya (`sprint4-screen-consistency.md:530`, Sprint 5'e ertelenmiş) |
| *"tek başarılı execute **§12 ön kaydını bozar**"* | **Bozmaz.** §12 **nonce 4'ün** ön kaydıydı, §13'te tüketilip **kapandı** ve beklentisi birebir tuttu. Risk altındaki şey nonce 5'in demoya ayrılmış olmasıydı — sonuç aynı, gerekçe farklı. Ayrıca **nonce 5 için hiç ön kayıt yoktu**; o boşluk bu oturumda kapatıldı |
| *"negatif kanıt MetaMask'te onaylanırsa zincire tx gider mi"* | **Öyle bir yol yok.** Buton `preflight()` → `signer.call()` → `eth_call` çağırıyor; onay penceresi **hiç açılmıyor**. Koddan okundu **ve** 24 Eylül'de tarayıcıda gözlendi |

Dördüncü bir düzeltme de Cowork'ten geldi ve **doğruydu**: *"commit bloktan
73 dk önde"* hesabı commit tarihine dayanıyordu; commit tarihi istemci
üretimidir. Push zamanına çevrildi ve sunucu tarafı damga (`pushed_at`) ile
sabitlendi. **İyi yakalama.**

## C.2 Rapor aşamasında incelenmesini istediğim kararlar

1. **EIP eşleştirmeleri** (§ A.3 #1). Hafızadan yazıldı, spec'e karşı
   doğrulanmadı. İkinci bir gözle bakılsın — rapora kaynaksız sayı girmesin.
2. **"Üç fark da tam oturdu" hükmünün gücü.** Her satır **tek** koşu,
   tekrarlanabilirlik ölçülmedi, `nonce++` maliyeti üç koşuda aynı
   **varsayıldı**. Raporda bu sınırın nasıl ifade edileceği tartışılsın —
   "ölçüldü ve tam tuttu" ile "üç örnekte tam tuttu" arasındaki fark.
3. **Foundry ↔ Sepolia 2,5 kat çelişkisi** (§ A.4). Hakan'ın cevabı gelmezse
   rapora ne yazılacak? Plan "köprü yazılmaz" diyor; jüri sorarsa cevap ne?
4. **Sınanmamış yollar tablosu.** `receipt.status === 0` iddiası hâlâ
   **kaynak okumasına** dayanıyor, ampirik gözleme değil (`PQWallet.sol:48`).
   `ZİNCİRDE REVERT` ve `SONUÇ ALINAMADI` etiketleri de hiç gözlenmedi.
   Raporda bu üçü birlikte mi sunulsun?
5. **Yan bulgunun ifadesi.** *"Bizim `gasLimit`'imiz zincire ulaşmıyor"*
   beş koşuda tutarlı, ve son iki koşuda dayanak güçlendi. Ama
   *"MetaMask kendi ham tahminini yazıyor"* alternatifi **elenmedi**
   (MetaMask'e giden istek kaydedilmedi). Rapora hangi kuvvette girsin?
6. **Video kanıtının sunumu.** Kayıt tek çekim, SHA-256 alındı, kayıt
   anındaki kaynak md5'leri de. Jüriye nasıl sunulacağı kararlaşmadı.

---

## Bu notun kendi sınırı

Buradaki her sayı ve durum **25 Eylül 2026'da ölçüldü**. Depo ve zincir
durumu değişmiş olabilir; oturum açan ajan önce § A.1'i yeniden ölçsün.
